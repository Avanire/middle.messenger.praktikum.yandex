.form-wrapper {
    &__inner {
        background: var(--white-color);
        box-shadow: 0 0 6px rgba(0, 0, 0, 0.14);
        border-radius: 12px;
        width: 340px;
        padding: 50px 30px;
        min-height: 450px;
        display: flex;
        flex-direction: column;
    }

    &__title {
        text-align: center;
    }
}
